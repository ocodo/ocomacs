;;(use-package treesit-auto
  ;;:custom
  ;;(treesit-auto-install t)
  ;;:config
  ;;(treesit-auto-add-to-auto-mode-alist 'all)
;;(global-treesit-auto-mode))

;;; Great but very annoying when it tries to setup for bash fails,
;;; overrides sh-mode and ... in general makes a mess of things.
;;  M-x global-treesit-auto-mode is enough to get going until the bash
;;  issue gets some time to sort out...
